# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Pristine client-supplied artwork (Drive originals, ~70MB). Kept locally so the
# web assets can be regenerated, but never committed — Drive is the archive.
# NOTE: deliberately OUTSIDE public/ — Vite copies public/ verbatim into the
# build, so originals left there would ship 70MB to production.
# See public/assets/img/bluverse/README.md
design-assets/
